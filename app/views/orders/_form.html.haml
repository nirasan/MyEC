= simple_form_for(@order) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :address
    = f.input :zip_code
    = f.input :phone_number

  .form-actions
    = f.button :submit
